<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
      <div class="span2">
        <%= f.label :user %>
      </div>
      <div class="span8">
        <%= f.select :user_id, @user_list.map {|r| [r.name,r.id] } %>
      </div>
  </div>
  <div class="row">
    <div class="span2">
      <%= f.label :content %>
    </div>
    <div class="span8">
      <%= f.text_area :content, :class => 'form-textarea' %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
